#include "main.h"

/**
 * mul - multiplies two ints
 * @a: first int for the multiplication
 * @b: second int for the multiplication
 *
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
