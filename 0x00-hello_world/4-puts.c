#include <stdio.h>

/**
 * main - prints the given string on the stdout
 *
 * Return: always 0, to the operating system if successful
 */
int main(void)
{
	char *quote = "\"Programming is like building a multilingual puzzle";

	puts(quote);
	return (0);
}
