#include <stdio.h>

/**
 * main - prints the given quote to the stdout
 *
 * Return: always 0 if successful
 */
int main(void)
{
	char *quote = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", quote);
	return (0);
}
