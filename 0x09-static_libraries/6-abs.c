#include "main.h"

/**
 * _abs - computes the absolute value of a given number
 * @n: the number which abs value is to be found
 *
 * Return: the abs of n
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);

	return (n);
}
