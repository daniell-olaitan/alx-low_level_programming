#include "main.h"

/**
 * _puts - prints a given string to the stdout
 * @str: the string to be printed
 *
 */
void _puts(char *str)
{
	int i;

	for (i = 0; s[i]; ++i)
		_putchar(s[i]);

	_putchar('\n');
}
