#ifndef DEF_H
#define DEF_H

/**
 * _strlen - computes len of str
 * @str: the given str
 *
 * Return: len of str
 */
unsigned int _strlen(char *str)
{
	unsigned int i;

	for (i = 0; str[i]; ++i)
	{
	}

	return (i);
}
